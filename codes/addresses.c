#include <stdio.h>

int main(void)
{
    int n = 50;
    int *p = &n; // p is pointer var that stores the address of a int. &n is the address of n

    printf("%p\n",p); // print address of n
    )

}
